const axios = require('axios');

async function run() {
  try {
    // We don't have the API running locally unless I start it.
    // I should just inspect the code first.
  } catch(e) {
    console.error(e);
  }
}
run();
